---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: production
  namespace: flux-system
spec:
  chart:
    spec:
      chart: charts/app
      sourceRef:
        kind: GitRepository
        name: deploy
  interval: 30s
  targetNamespace: production
  values:
    image:
      tag: 1.18.0